package cn.mapway.ui.shared;

public enum DesignEventType {
    Layout,
    Select,
    Move,
    Delete,
    Add,
    Up,
    Down,
    Ok,
    Cancel,
    Menu,
    Mode,
    Update,
    Draw,
    Resize,
    Refresh,
    Create,
    Locked;
}
