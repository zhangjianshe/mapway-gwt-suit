package __PACKAGE__;

import __BEANPACKAGE__.__NAME__Request;
import __BEANPACKAGE__.__NAME__Response;
import cn.mapway.biz.core.AbstractBizExecutor;
import cn.mapway.biz.core.BizContext;
import cn.mapway.biz.core.BizRequest;
import cn.mapway.biz.core.BizResult;

import cn.mapway.ui.client.IUserInfo;
import cn.mapway.ui.shared.CommonConstant;
import lombok.extern.slf4j.Slf4j;
import org.nutz.json.Json;
import org.nutz.json.JsonFormat;
import org.nutz.lang.Strings;
import org.springframework.stereotype.Component;

/**
 * __NAME__Executor
 *
 * @author zhangjianshe <zhangjianshe@gmail.com>
 */
@Component
@Slf4j
public class __NAME__Executor extends AbstractBizExecutor<__NAME__Response, __NAME__Request> {
    @Override
    protected BizResult<__NAME__Response> process(BizContext context, BizRequest<__NAME__Request> bizParam) {
        __NAME__Request request = bizParam.getData();
        log.info("__NAME__Executor {}", Json.toJson(request, JsonFormat.compact()));
        LoginUser user= (LoginUser) context.get(CommonConstant.KEY_LOGIN_USER);

        return BizResult.success(null);
    }
}
